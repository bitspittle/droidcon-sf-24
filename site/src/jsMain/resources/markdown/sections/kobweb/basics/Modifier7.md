---
layout: center
---

## ${FaCodeFork()} Modifiers are a fork!

{{{ Horizontal

```kotlin
Modifier
    .padding(20.px)
    .fillMaxWidth()
```

```kotlin
Modifier
    .fillMaxWidth()
    .padding(20.px)
```

```kotlin
Modifier
    .padding(20.px)
    .fillMaxWidth()
    .padding(20.px)
```

}}}

### ${Kobweb()} != ${AndroidText("Jetpack Compose")}
